the simple commands for docker
```shell
docker images  # show local docker images
docker save e0c985 -o nginx_latest.tar  # package local image of which the image id's prefix is e0c985
docker load -i python3.11.tar # load image to current machine
docker tag 123abc456def your-repo:your-tag  # change the repository and tag for the image
```